scrape_configs:
  - job_name: 'pillar-controller'
    scrape_interval: 15s
    metrics_path: '/metrics'
    static_configs:
      # Controller runs on the macOS host, not in the container network.
      - targets: ['host.docker.internal:8080']
